pipeline {
    agent any 
    environment {
      HOME='.'
      CI = 'true'
    }
    options {
        disableConcurrentBuilds()
    }
    stages {
        stage('Build') {
          steps {
            sh '''
              echo "Building files"
              zip -r ../NewGitLab.zip ./
            '''
          }
        }
        stage('Deploy') {
          steps {
            withAWS(credentials: 'Jenkins S3', region: 'us-east-1') {
              s3Delete bucket: 'trainingbucket3', path: '**/*'
              s3Upload acl: 'Private', bucket: 'trainingbucket3', file: 'NewGitLab.zip', workingDir: '../'
              s3Delete bucket: 'trainingbucket3', path: 'NewGitLab.zip'
              s3Upload bucket: 'trainingbucket3', file: '../NewGitLab.zip'
            }
          }
        }

    }
}
